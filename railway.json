{
  "build": {
    "builder": "node"
  },
  "deploy": {
    "start": "node index.js"
  },
  "env": {
    "TOKEN": {
      "description": "Your Discord bot token",
      "required": true
    },
    "PREFIX": {
      "description": "Prefix for bot commands",
      "required": true
    }
  }
}
